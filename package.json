{
  "name": "benchmark-dashboard-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd api && npm install && cd ../ui && npm install && npm run build",
    "start": "node api/server.js"
  }
}
