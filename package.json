{
  "name": "benchmark-dashboard-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install-all": "npm install --prefix api && npm install --prefix ui",
    "build": "npm run install-all && npm run build --prefix ui",
    "start": "node api/server.js"
  }
}
